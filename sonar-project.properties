sonar.projectKey=JuanCVO_Project-Mancala-Inteligencia-Artificial
sonar.organization=projectmancala
# Sonar analiza backend (Python) y frontend (JS/CSS/HTML) + Dockerfiles.
# El motor C++ se valida en el workflow motor-ci (compilacion + ctest); no se
# analiza aqui porque el analizador C/C++ de Sonar necesita el compilador dentro
# del contenedor del scanner, lo que queda fuera del alcance de este proyecto.
sonar.sources=backend/app,frontend
sonar.tests=backend/tests
sonar.python.version=3.11
